# Email de contact affiché publiquement (footer, mailto).
CONTACT_EMAIL=contact@chom.re

# Destinataire des demandes, journalisé si aucun webhook n'est configuré.
DEVIS_RECIPIENT=contact@chom.re

# Optionnel : URL de webhook où acheminer les leads (Slack incoming webhook,
# Make/n8n, service de transfert vers email…). Si vide → journalisation seule.
DEVIS_WEBHOOK_URL=
